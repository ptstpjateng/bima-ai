# ── Shared database credentials ──────────────────
DB_DATABASE=bima_ai
DB_USERNAME=bima
DB_PASSWORD=changeme_in_production

# ── Frontend ──────────────────────────────────────
# Public URL of the Laravel backend (as seen from the browser)
NEXT_PUBLIC_API_URL=http://localhost:8080
