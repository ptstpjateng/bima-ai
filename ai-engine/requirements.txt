fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
requests==2.32.3
httpx==0.28.1
# Direct Postgres driver for the SIAP tool layer (services/siap_db.py).
# Read-only reads against dbsiapjateng — same driver admin-api uses.
asyncpg>=0.30.0,<0.31
chromadb==0.6.3
# Pinned to avoid huggingface_hub API breakage (is_offline_mode removed in >=0.28)
huggingface-hub==0.27.0
transformers==4.47.0
sentence-transformers>=3.0,<6.0
# Multi-page PDF reader for the officer-copilot doc-Q&A tools
# (services/agents/doc_qa.py). Pure Python, no system deps — fits the slim
# ai-engine container.
pypdf>=5.0,<6.0
