fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.1
python-dotenv==1.0.1
requests==2.32.3
httpx==0.28.1
# Async Redis client for durable chat sessions (services/session_store.py).
# Uses redis.asyncio (ships inside the `redis` wheel since 4.2 — no aioredis).
# Optional at runtime: session_store lazy-imports it and degrades to in-memory
# when absent, but it ships so BIMA_REDIS_SESSIONS_ENABLED works on the VPS.
redis>=5.0,<6.0
# Direct Postgres driver for the SIAP tool layer (services/siap_db.py).
# Read-only reads against dbsiapjateng — same driver admin-api uses.
asyncpg>=0.30.0,<0.31
chromadb==0.6.3
# Pinned to avoid huggingface_hub API breakage (is_offline_mode removed in >=0.28)
huggingface-hub==0.27.0
transformers==4.47.0
sentence-transformers>=3.0,<6.0
# JWT verification for citizen-SSO tokens on /webhook/chat (security gate).
# Pinned to the same range admin-api uses so a JWT minted there validates
# here byte-for-byte. Keep them in lock-step.
python-jose[cryptography]>=3.3.0,<4
